* add the ability to pause logging
  * this should close the log file, to allow the user to edit it manually
  * right now I have to kill the server and restart it for this
* convert line endings to native format if posted data is text/*
* in greaselog.js, handle the case of docs with no titles
  * [example](http://www.rfc-editor.org/bcp/bcp178.txt)
* detect errors in log submission and report them to the user
